SERP_SEARCH_API_KEY=      # Required: SerpAPI key for Google search (https://serpapi.com)
OPENAI_API_KEY=           # Optional: enables live quiz generation; falls back to mock data if unset
LOG_LEVEL=info            # Optional: pino log level (trace|debug|info|warn|error), default: info
